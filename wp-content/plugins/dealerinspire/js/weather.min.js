jQuery(document).ready(function(a) {
    if (weather && weather.resp) {
        var b = weather.resp;
        if (b && b.main && b.main.temp) {
            var c = "imperial" == weather.units ? "F" : "C";
            a(".weather .temperature").html(Math.round(b.main.temp) + "&deg; " + c), a(".weather .icon img").attr("src", "/wp-content/themes/DealerInspireCommonTheme/images/mobile/weather/" + b.weather[0].icon + ".png"), a(".weather .summary").html(weather_status[b.weather[0].id][1].replace("{Temp}", Math.round(b.main.temp)))
        }
    } else a("div.weather").empty()
});