! function(a) {
    a.fn.placehold = function(d) {
        function b() {
            for (i = 0; i < arguments.length; i++) arguments[i].toggle()
        }
        var d = d || "placeholder",
            c = a.fn.placehold.is_supported();
        return c ? this : this.each(function() {
            var f = a(this),
                e = f.attr("placeholder");
            if (e) {
                if ("" !== f.val() && f.val() != e || f.addClass(d).val(e), f.is(":password")) {
                    var g = a("<input />", {
                        "class": f.attr("class") + " " + d,
                        value: e
                    });
                    g.bind("focus.placehold", function() {
                        b(f, g), f.focus()
                    }), f.bind("blur.placehold", function() {
                        "" === f.val() && b(f, g)
                    }), f.hide().after(g)
                }
                f.bind({
                    "focus.placehold": function() {
                        f.val() == e && f.removeClass(d).val("")
                    },
                    "blur.placehold": function() {
                        "" === f.val() && f.addClass(d).val(e)
                    }
                }), f.closest("form").bind("submit.placehold", function() {
                    return f.val() == e && f.val(""), !0
                })
            }
        })
    }, a.fn.placehold.is_supported = function() {
        return "placeholder" in document.createElement("input")
    }
}(jQuery);